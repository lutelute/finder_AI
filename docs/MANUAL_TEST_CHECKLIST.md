# FinderAI Workspace 実機チェックリスト

テスト日、macOS、ディスプレイ構成を記録し、未実施を合格扱いにしないでください。まず重要なファイルではなく、一時テストフォルダで行います。

## 起動とレイアウト

- [ ] `/Applications/FinderAI.app`が一つの通常ウインドウで開く
- [ ] 設定下部のversion・build・commitがビルドしたInfo.plistと一致する
- [ ] `dist/FinderAI.app`から開いた場合は「開発用コピー」と表示される
- [ ] 起動直後からサイドバー、一覧、検索、下部`TERMINAL`を操作できる
- [ ] Accessibility設定を要求しない
- [ ] ウインドウresizeで一覧とTerminalの左右端が常に一致する
- [ ] サイドバーdividerを160〜360ptで動かせる
- [ ] 最小820×520ptでも操作不能な重なりがない
- [ ] フルスクリーンと通常表示を往復できる
- [ ] 再度アプリを開いてもmain windowが増殖しない

## ナビゲーションと一覧

- [ ] ホーム、デスクトップ、書類、ダウンロード、GitHub、Macintosh HDへ移動できる
- [ ] 戻る、進む、親フォルダ、パンくずが正しい
- [ ] フォルダをダブルクリックすると同じwindow内で移動する
- [ ] ファイルをダブルクリックすると既定アプリで開く
- [ ] 名前／変更日／サイズ／種類のsortが動く
- [ ] 現在フォルダ検索が即時に絞り込み、解除で全件に戻る
- [ ] 検索範囲を「配下」にすると子孫が相対パス付きで見つかり、打ち直した古い結果が後から混ざらない
- [ ] リスト／カラム／ギャラリーを直接切り替えられ、`⌘2`／`⌘3`／`⌘4`が各モードへ直接移り、`⌘⌥2`で3モードを循環する
- [ ] `⌘D`が複製、`⌃⌘T`がピン留めになっている（旧`⌘D`＝ピン留めではない）
- [ ] ファイルメニューから 情報を見る（`⌘I`）・複製・エイリアスを作成・圧縮 を実行できる
- [ ] 未選択のとき複製とエイリアスがグレーアウトし、情報を見ると圧縮は現在フォルダに効く
- [ ] 既定サイズのウインドウでサイドバーを160〜360ptのどこへ動かしても、一覧に横スクロールバーが出ず、名前カラムの右端が種類カラムの右端と揃う
- [ ] 長いファイル名が、種類カラムに余白を残したまま省略されない
- [ ] ウインドウを最小幅（820pt）まで縮めた場合は横スクロールバーが出てよい（各列の最小幅の合計が入らないため）
- [ ] ギャラリーで複数選択、ダブルクリック、Space、右クリック、ドラッグ開始が動く
- [ ] 2画面分割の狭いペインでも、2段のナビゲーションと検索欄が重ならない
- [ ] 再読み込みで外部変更を反映する
- [ ] 読めないフォルダで固まらずエラーを表示する

## 安全なファイル整理

- [ ] 新規フォルダが`新規フォルダ`、衝突時は`新規フォルダ 2`として作られる
- [ ] 改名後に項目が再選択される
- [ ] 既存名への改名は上書きせず拒否する
- [ ] dragで移動、Option+dragでコピーになる
- [ ] destinationに同名項目がある場合、どちらも変更せず拒否する
- [ ] フォルダを自身や子孫へdropできない
- [ ] 複数選択のゴミ箱操作で確認が出る
- [ ] ゴミ箱からFinderで復元できる
- [ ] 空白、日本語、引用符、`$()`を含む名前をshell評価せず扱える

## 画面端のフォルダ（エッジタブ）

- [ ] `⌃⌘E`で現在フォルダがタブになり、最初の1個で機能ごと有効になる
- [ ] タブに0.3秒カーソルを乗せると開き、離れて0.4秒で畳まれる
- [ ] タブとポップアップのあいだをカーソルが渡っても閉じない
- [ ] 端をただ通り過ぎただけでは開かない
- [ ] 他アプリを前面にしたままタブを使え、押してもFinderAIが前面に出ない
- [ ] タブのクリックでFinderAIがそのフォルダを開く
- [ ] 一覧のファイルのダブルクリックで関連アプリが開く
- [ ] フォルダのダブルクリックでその場で降り、`‹`で戻る（ウインドウは前面に出ない）
- [ ] 一覧からファイルを他アプリへドラッグして取り出せる
- [ ] タブへのドロップで入り、受け取れるときだけタブが光る
- [ ] 一覧へのドロップで入り、Optionでコピー、同名は拒否される
- [ ] ドロップ先を探しているあいだにポップアップが畳まれない
- [ ] 自分自身や、いま居るフォルダへの移動は受け付けない
- [ ] セッションが走っているフォルダのタブに青い点が出て、終了で消える
- [ ] タブの右クリック「このフォルダのTerminalを開く」でそのセッションが前面に出る
- [ ] 画面上端・下端に近いタブでも、ポップアップが画面内に収まる
- [ ] 8個登録した状態で`⌃⌘E`すると上限を伝えるシートが出る
- [ ] タブの右クリックから外せ、全部外すと帯ごと消える
- [ ] 表示メニューで左右を切り替えると、タブの角の落とし方も反対になる
- [ ] 「画面端のフォルダを表示／隠す」で隠しても、戻すと同じ並びが出る
- [ ] `⌘,`の「画面端のフォルダ」から表示・左右・自動的に隠すを変えられ、即座に効く
- [ ] `⌘,`の一覧から登録フォルダを外せる（隠す設定でタブが画面に無くても外せる）
- [ ] `⌘,`の「パネルの位置」を変えると、開いている全ウインドウのTerminalが移る
- [ ] 「自動的に隠す」でタブが画面外へ引っ込み、縁にカーソルを当てると滑り出る
- [ ] 滑り出したあとタブに触れずにカーソルを離しても、帯がひとりでに引っ込む
- [ ] 隠れているあいだ、画面端のクリック（他アプリのスクロールバー等）を奪わない
- [ ] 画面を横切るだけの動きでは滑り出さない
- [ ] ポップアップを覗いているあいだは帯が引っ込まない
- [ ] Mission Control、Stage Manager、他アプリのフルスクリーンでの見え方
- [ ] 外部ディスプレイの抜き差しと解像度変更で帯が破綻しない
- [ ] 読めないフォルダ（アクセス未許可）で「読み取れません」と出る
- [ ] 追加の権限ダイアログが出ない
- [ ] タブの`⌃`クリックでリスト／アイコン表示と並び順を変えられ、開いたまま切り替わる
- [ ] タブの`⌘`クリックで上の階層が並び、選ぶとそこが開く
- [ ] 一覧でスペースを押すとクイックルックが出て、もう一度押すと閉じる
- [ ] 一覧の右クリックから、情報・Finderで表示・複製・エイリアス・圧縮・改名・新規フォルダ・ゴミ箱が使える
- [ ] `⌘C`→別フォルダで`⌘V`でコピーでき、同名は拒否される
- [ ] `⌘⌫`でゴミ箱に入る（確認なしで消えたりしない）
- [ ] 矢印キーで選択が動き、`→`で降り、`←`で戻り、escで閉じる

## たくさん開いたときの見分け

- [ ] 同名フォルダを2枚以上開くと副題に親フォルダ名が出て、1枚に戻すと消える
- [ ] ウインドウメニューに開いている全ウインドウが並び、選ぶとそれが前に出る
- [ ] 画面端のタブから開くとき、すでに開いているウインドウがあれば新しく開かずそれが前に出る

## Terminal

- [ ] 閲覧だけではShell／Codex／Claudeプロセスが増えない
- [ ] `⌘J`、status button、header buttonのすべてで開閉できる
- [ ] 上端dragで160〜600ptに変更できる
- [ ] 畳んだ状態でheaderの空き地をclickすると開き、開いた状態のsingle clickでは閉じない
- [ ] headerのdouble click（`⌘⇧J`）で「半分→最大→畳む」と一段ずつ巡る
- [ ] header右の配置button（`⌘⌥J`）で右辺へ移り、左端dragで280〜720ptに変更できる
- [ ] 右辺で畳むと34ptの縦stripになり、実行中件数のbadgeが出る
- [ ] 右辺と下辺を往復しても、それぞれの大きさが保たれる
- [ ] 右辺のままFinderAIを再起動すると右辺で復元する
- [ ] 右辺＋`⌃⌘S`の2画面分割でも、一覧が読める幅を保ったままterminalが縮む
- [ ] `Shell`でzshが現在フォルダから始まり、`pwd`が一致する
- [ ] `echo test`、日本語入力、copy、pasteが動く
- [ ] `＋`からShell／Codex／Claudeを選べる
- [ ] CLIなしの場合は対応項目が無効で、自動インストールしない
- [ ] 長い処理中に閉じても継続する
- [ ] 実行中タブを右クリックして「タブを隠す」を選ぶとタブだけが消え、プロセスは継続する
- [ ] `⌘⌥T`の管理画面で隠したsessionが「バックグラウンド」と表示され、「タブに表示」で同じバッファへ戻る
- [ ] セッションセンターで名前・種類・フォルダ・状態の検索、5種類の状態絞り込み、名前変更、ピン留め／解除が動く
- [ ] ピン留めしたsessionが先頭へ移動し、再起動後も名前・ピン・最終活動が残る
- [ ] FinderAIを通常終了・再起動しても管理画面に前回のsession履歴が残り、「記録を削除」で履歴だけを消せる
- [ ] 壊れた`session-registry.json`が隔離され、FinderAIは空の台帳で通常起動する
- [ ] タブの右クリックと管理画面の両方から、現在の表示バッファを明示した保存先へテキスト保存できる
- [ ] tmux永続化をオフにした後も残存sessionが管理画面に出て、「タブに表示」で再接続できる
- [ ] FinderAI終了後も残るFinderAI名義tmuxが次回起動で「待機中」と表示され、同じsessionへ再接続できる
- [ ] 台帳にだけ残りtmuxから消えた項目は「消失」、tmux確認不能時は「記録済み（未照合）」になる
- [ ] 別フォルダへ移動しても既存sessionへ`cd`が送られない
- [ ] 元フォルダへ戻るとsession tabが再表示される
- [ ] 実行中sessionを閉じると確認が出る
- [ ] app終了のキャンセルでsessionが継続する
- [ ] app終了後も外部Terminal／Codex／Claudeは終了しない

## 障害時

- [ ] Documentsアクセスを拒否してもappとFinderが通常動作する
- [ ] File Provider待機中もmain windowが操作可能でspinnerが表示される
- [ ] 外部で表示中フォルダを削除してもクラッシュしない
- [ ] appを強制終了してもFinderへ影響しない

## 旧Finder overlayを使う場合だけ

- [ ] Accessibility許可後にFinder下端へbarが一つ表示される
- [ ] Finder移動、resize、最小化、複数window、Space切替へ追従する
- [ ] Finder自体のframeを変更しない
- [ ] overlayの制約を許容できない場合はWorkspace版へ戻る

## 2026-08-08 の変更ぶん（PR #35〜#52）

順に上から。**1〜3は今日の主訴そのもの**なので、ここが駄目なら他は後回しでよいです。
各項目の末尾に、こちらで実機確認できたか（済／未）を付けています。

**2026-08-08、使う人の実機で 1〜3 は全て合格を確認しました。** 4以降は未確認です。

### 1. Terminalの文字が読めるか（朝の主訴）  ✅ 実機合格

- [ ] 適当なフォルダでClaudeを起動し、**文字が読める**（暗い画面に暗い文字、が起きない）【済／要日常確認】
- [ ] 日本語・絵文字・罫線が`_`に化けない（`ls`や`claude`の画面で確認）【済】
- [ ] ターミナル下端に緑のtmuxステータス行が出ていない【済】
- [ ] 一覧とターミナルで明るさを別々に切り替えても、文字が読めるまま【未】

### 2. AIに戻れるか  ✅ 実機合格

- [ ] FinderAIを終了→起動し、生きているtmuxセッションが「実行中N件（うちM件は未接続）」と数えられている【済】
- [ ] 同じフォルダで「Claudeに再接続」を押すと、前の会話がそのまま出る【済】
- [ ] 会話の記録があるフォルダで「**Claudeで前回の続き**」を押すと直前の会話へ戻る【済】
- [ ] 戻れない場合でも、断りが1行出てから**新しい会話が始まる**（タブが一瞬で消えない）【済】
- [ ] Codexでも「前回の続き」が同じように動く【未】

### 3. タブが見分けられるか（深夜のご指摘）  ✅ 実機合格

- [ ] 種類ごとの記号と色で、Shell／Codex／Claudeが読まずに見分けられる【済】
- [ ] **今いるフォルダのセッションが帯の先頭**にいる【済】
- [ ] タブを増やしていくと、名前→フォルダ省略→記号+2文字、と段階的に詰まる【済】
- [ ] 入り切らないぶんが「**＋N**」で出て、押すとセッションセンターが開く【未（表示は確認済み・押した先は未）】
- [ ] タブが増えてもパネルが横に膨らまず、ファイル一覧が潰れない【済】
- [ ] `⌘⌥→` `⌘⌥←` でセッションを前後に移れる【済】

### 4. 変わったショートカット（旧キーは効かなくなっています）

- [ ] `⌘⇧T` でセッションセンターが開く（旧 `⌥⌘T`）【済】
- [ ] `⌃⌘S` で2画面分割（旧 `⌥⌘S`）【済】

### 5. 名前と役割

- [ ] タブの右クリック「名前と役割…」で名前を付けると、**タブがその名前になる**【済】
- [ ] 役割を書いて保存し、そのセッションを終了→開始し直すと、**役割どおりに振る舞う**【済】
- [ ] 役割を持つタブに `✳︎` が付く【済】
- [ ] `⌘⇧T` の一覧に「役割」列があり、役割の文言でも検索できる【済（列表示のみ）】
- [ ] Codexのタブには役割欄が出ない（名前だけ）【未】

### 6. Finderとの受け渡し

- [ ] `open -a FinderAI <フォルダ>` でそのフォルダの窓が開く【済】
- [ ] ファイルを渡すと、入れ物のフォルダが開いて**そのファイルが選択される**【済】
- [ ] Finderの右クリック「このアプリケーションで開く」に FinderAI が出る【未】
- [ ] Dockアイコンへのドロップでも開く【未】
- [ ] **既に開いている場所を渡しても、見ていた窓が差し替わらない**【済】
- [ ] 設定（`⌘,`）の「開く先のフォルダ」を決めると、`⌘N` がそこで開く【済】

### 7. 複数ウインドウ

- [ ] 同じフォルダを2つの窓で開いても、**片方のAI画面が消えない**【済】
- [ ] タブを押したときだけ、その窓へAIの中身が移る【済】
- [ ] 2画面分割を解除したあと、**ウインドウ名が今見ているフォルダと一致する**【済】

### 8. こちらで確認できていないショートカット

自動操作では確かめきれませんでした。日常で使っていて**反応が無いものがあれば教えてください**。

- [ ] `⌃⌘E` このフォルダを画面端に置く／外す
- [ ] `⌘⌥C` パスをコピー ／ `⌘L` パス欄へフォーカス
- [ ] `⌘I` 情報を見る ／ `Space` クイックルック
- [ ] `⌘D` 複製 ／ `⌃⌘A` エイリアス ／ `⌘⇧N` 新規フォルダ
- [ ] `⌘Z` 取り消す ／ `⌘F` 検索欄
- [ ] `⌘[` `⌘]` `⌘↑` 戻る・進む・親フォルダ
- [ ] `⌘⇧F` Finderの現在地を開く
